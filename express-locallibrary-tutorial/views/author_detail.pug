extends layout

block content
    h1= 'Author: ' + author.name
    p= author.lifespan

    if author_books.length
        div(style='margin-left:20px;margin-top:20px')
            h4 Books

            dl
                each book in author_books
                    dt
                        a(href=book.url)= book.title
                    dd= book.summary
    else
        p No titles associated with this author

    hr
    a.btn.btn-danger(href=author.url + '/delete') Delete Author
    a.btn.btn-secondary.ml-3(href=author.url + '/update') Update Author

    // Path: views/author_detail.pug