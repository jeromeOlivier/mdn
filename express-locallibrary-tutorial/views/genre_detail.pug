extends layout

block content
    h1= title

    dl
        each book in genre_books
            dt
                a(href=book.url) #{book.title}
            dd #{book.summary}

        else
            p There are no titles associated with this genre